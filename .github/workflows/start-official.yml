name: Start official scoring on Pages

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages

      - name: Fetch start script from main
        run: |
          curl -fsSL -o /tmp/start-official.py \
            https://raw.githubusercontent.com/CrypThias6/learning-hub/main/scripts/start-official.py
          python3 -m py_compile /tmp/start-official.py

      - name: Patch live bundle
        run: |
          python3 /tmp/start-official.py \
            _expo/static/js/web/index-e692932e42ea2ff98944c1bb62b893a7.js
          sed -i 's|index-e692932e42ea2ff98944c1bb62b893a7.js[^"]*|index-e692932e42ea2ff98944c1bb62b893a7.js?v=live2|' index.html

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add _expo/static/js/web/index-e692932e42ea2ff98944c1bb62b893a7.js index.html
          git commit -m "Turn official scoring on" || echo no change
          git push origin gh-pages
